<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Emonado{% endblock %}</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        body {
            padding-top: 70px;
            background-image: url('{{ asset('images/background.png') }}');
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
            background-attachment: fixed;
            min-height: 100vh;
        }

        .navbar-logo {
            height: 36px;
            width: auto;
        }

        .navbar-brand span {
            font-weight: bold;
            font-size: 1.3rem;
            color: white;
        }
    </style>

    {% block stylesheets %}{% endblock %}
</head>

<body>

<nav class="navbar navbar-expand-lg navbar-dark bg-success fixed-top">
    <div class="container">

        <!-- ✅ LOGO + TEXTE (ROUTE EXISTANTE UNIQUEMENT) -->
        <a class="navbar-brand d-flex align-items-center gap-2"
           href="{{ path('app_journal_index') }}">
            <img src="{{ asset('images/logo.png') }}"
                 alt="Logo"
                 class="navbar-logo">
            <span>Emonado</span>
        </a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto">

                <!-- ✅ ROUTE EXISTANTE -->
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_journal_index') }}">Accueil</a>
                </li>

                <!-- ❌ Routes non encore créées → désactivées -->
                {#
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('test_page') }}">Test</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_login') }}">Connexion</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_register') }}">S'inscrire</a>
                </li>
                #}

            </ul>
        </div>
    </div>
</nav>

<div class="container mt-5">
    {% block body %}{% endblock %}
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
{% block javascripts %}{% endblock %}

</body>
</html>
