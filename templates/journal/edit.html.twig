{% extends 'base.html.twig' %}

{% block title %}Edit Journal{% endblock %}

{% block body %}
    <h1 class="mb-4">Modifier le journal</h1>

    {{ include('journal/_form.html.twig', {'button_label': 'Enregistrer', 'journal': journal}) }}

    <div class="mt-3">
        <a class="btn btn-outline-secondary" href="{{ path('app_journal_index') }}">Retour a la liste</a>
    </div>

    <div class="mt-2">
        {{ include('journal/_delete_form.html.twig') }}
    </div>
{% endblock %}
