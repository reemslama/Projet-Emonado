{% extends 'base.html.twig' %}

{% block title %}Page de démo{% endblock %}

{% block body %}
<div class="container py-4">
  <h1 class="mb-3">Page de démo</h1>
  <p class="text-muted">Contrôleur : {{ controller_name|default('TestController') }}</p>
  <p>Cette page est une page de démonstration. Pour accéder aux questionnaires (stress, dépression, QI), utilisez le lien ci-dessous.</p>
  <div class="mt-3 d-flex gap-2 flex-wrap">
    <a class="btn btn-primary" href="{{ path('test_page') }}">Choisir un questionnaire</a>
    <a class="btn btn-outline-secondary" href="{{ path('home') }}">Retour à l’accueil</a>
  </div>
</div>
{% endblock %}
