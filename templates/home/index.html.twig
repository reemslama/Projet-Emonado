{% extends 'base.html.twig' %}

{% block title %}Emonado - Évaluez votre bien-être{% endblock %}

{% block body %}
<style>
    /* Override du body de base.html.twig */
    body {
        background-image: url('{{ asset('images/background.png') }}');
        background-size: cover;
        background-position: center;
        background-repeat: no-repeat;
        background-attachment: fixed;
        min-height: 100vh;
        position: relative;
    }

    /* Overlay PLUS SOMBRE pour améliorer la lisibilité */
    body::before {
        content: '';
        position: fixed;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: rgba(0, 0, 0, 0.4);  /* ✅ Plus sombre (était blanc transparent) */
        z-index: -1;
    }

    .hero-section {
        min-height: 85vh;
        display: flex;
        align-items: center;
        justify-content: center;
        text-align: center;
        padding: 60px 20px;
    }

    .hero-content {
        background: rgba(255, 255, 255, 0.98);  /* ✅ Légèrement plus opaque */
        padding: 60px 40px;
        border-radius: 30px;
        box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);  /* ✅ Ombre plus prononcée */
        max-width: 800px;
        backdrop-filter: blur(10px);
    }

    .hero-title {
        font-size: 3.2rem;
        font-weight: 700;
        margin-bottom: 25px;
        color: #198754;
        line-height: 1.3;
    }

    .hero-subtitle {
        font-size: 1.25rem;
        margin-bottom: 0;
        color: #495057;
        line-height: 1.8;
    }

    /* Section caractéristiques */
    .features-section {
        background: rgba(255, 255, 255, 0.98);  /* ✅ Plus opaque */
        padding: 80px 20px;
        backdrop-filter: blur(10px);
    }

    .section-title {
        text-align: center;
        font-size: 2.5rem;
        font-weight: 700;
        color: #198754;
        margin-bottom: 60px;
    }

    .feature-card {
        text-align: center;
        padding: 40px 30px;
        background: white;
        border-radius: 20px;
        box-shadow: 0 5px 20px rgba(0, 0, 0, 0.08);
        transition: all 0.3s ease;
        height: 100%;
    }

    .feature-card:hover {
        transform: translateY(-10px);
        box-shadow: 0 15px 40px rgba(25, 135, 84, 0.2);
    }

    .feature-icon {
        font-size: 3.5rem;
        margin-bottom: 25px;
        color: #198754;
    }

    .feature-title {
        font-size: 1.5rem;
        font-weight: 600;
        margin-bottom: 15px;
        color: #2d3748;
    }

    .feature-description {
        color: #718096;
        line-height: 1.7;
        font-size: 1rem;
    }

    /* Section CTA */
    .cta-section {
        background: linear-gradient(135deg, #198754 0%, #157347 100%);
        padding: 80px 20px;
        text-align: center;
        color: white;
        position: relative;
    }

    .cta-section h2 {
        font-size: 2.5rem;
        font-weight: 700;
        margin-bottom: 20px;
    }

    .cta-section p {
        font-size: 1.2rem;
        margin-bottom: 40px;
        opacity: 0.95;
    }

    .cta-button {
        display: inline-block;
        background: white;
        color: #198754;
        padding: 18px 45px;
        border-radius: 50px;
        font-weight: 600;
        font-size: 1.1rem;
        text-decoration: none;
        transition: all 0.3s ease;
        margin: 0 10px 15px;
        box-shadow: 0 5px 20px rgba(0, 0, 0, 0.2);
    }

    .cta-button:hover {
        background: #f8f9fa;
        transform: translateY(-3px);
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
        color: #198754;
    }

    .cta-button i {
        margin-right: 8px;
    }

    /* Stats Section */
    .stats-section {
        background: rgba(255, 255, 255, 0.95);  /* ✅ Plus opaque */
        padding: 60px 20px;
        backdrop-filter: blur(10px);
    }

    .stat-card {
        text-align: center;
        padding: 30px 20px;
    }

    .stat-number {
        font-size: 3rem;
        font-weight: 700;
        color: #198754;
        margin-bottom: 10px;
    }

    .stat-label {
        font-size: 1.1rem;
        color: #6c757d;
        font-weight: 500;
    }

    /* Responsive */
    @media (max-width: 768px) {
        .hero-title {
            font-size: 2.2rem;
        }
        .hero-subtitle {
            font-size: 1.1rem;
        }
        .hero-content {
            padding: 40px 25px;
        }
        .section-title {
            font-size: 2rem;
        }
    }
</style>

{# Section Hero #}
<div class="hero-section">
    <div class="hero-content">
        <h1 class="hero-title">
            Bienvenue sur Emonado
        </h1>
        <p class="hero-subtitle">
            Votre plateforme de bien-être mental. Évaluez votre état psychologique 
            en quelques minutes et bénéficiez de recommandations personnalisées 
            pour améliorer votre qualité de vie.
        </p>
    </div>
</div>

{# Section Stats #}
<div class="stats-section">
    <div class="container">
        <div class="row">
            <div class="col-md-4 mb-4">
                <div class="stat-card">
                    <div class="stat-number">3</div>
                    <div class="stat-label">Tests Disponibles</div>
                </div>
            </div>
            <div class="col-md-4 mb-4">
                <div class="stat-card">
                    <div class="stat-number">10</div>
                    <div class="stat-label">Minutes Seulement</div>
                </div>
            </div>
            <div class="col-md-4 mb-4">
                <div class="stat-card">
                    <div class="stat-number">100%</div>
                    <div class="stat-label">Confidentiel</div>
                </div>
            </div>
        </div>
    </div>
</div>

{# Section Caractéristiques #}
<div class="features-section">
    <div class="container">
        <h2 class="section-title">Pourquoi choisir Emonado ?</h2>
        <div class="row">
            <div class="col-md-4 mb-4">
                <div class="feature-card">
                    <div class="feature-icon">
                        <i class="fa-solid fa-clipboard-check"></i>
                    </div>
                    <h3 class="feature-title">Tests Validés</h3>
                    <p class="feature-description">
                        Questionnaires scientifiques pour évaluer votre stress, 
                        dépression et capacités cognitives avec précision.
                    </p>
                </div>
            </div>
            <div class="col-md-4 mb-4">
                <div class="feature-card">
                    <div class="feature-icon">
                        <i class="fa-solid fa-user-shield"></i>
                    </div>
                    <h3 class="feature-title">Totalement Confidentiel</h3>
                    <p class="feature-description">
                        Vos données sont sécurisées et confidentielles. 
                        Seuls vous et votre psychologue y avez accès.
                    </p>
                </div>
            </div>
            <div class="col-md-4 mb-4">
                <div class="feature-card">
                    <div class="feature-icon">
                        <i class="fa-solid fa-chart-simple"></i>
                    </div>
                    <h3 class="feature-title">Suivi Personnalisé</h3>
                    <p class="feature-description">
                        Analyses détaillées et recommandations adaptées 
                        à votre profil pour un meilleur bien-être.
                    </p>
                </div>
            </div>
        </div>
    </div>
</div>

{# Section CTA #}
<div class="cta-section">
    <div class="container">
        <h2>Commencez votre parcours vers le bien-être</h2>
        <p>Inscrivez-vous gratuitement et accédez à tous nos outils d'évaluation psychologique</p>
        <a href="{{ path('app_register') }}" class="cta-button">
            <i class="fa-solid fa-user-plus"></i> Créer un compte
        </a>
        <a href="{{ path('app_login') }}" class="cta-button">
            <i class="fa-solid fa-right-to-bracket"></i> Se connecter
        </a>
    </div>
</div>

{% endblock %}