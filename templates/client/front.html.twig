<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Emonado - Espace Patient</title>
    <link rel="stylesheet" href="{{ asset('bootstrap/css/bootstrap.min.css') }}">
    <link rel="stylesheet" href="{{ asset('fontawesome/css/all.min.css') }}">
    <style>
        .hero-blue { background: #007bff; color: white; padding: 50px 0; }
        .search-card { margin-top: -30px; background: white; border-radius: 12px; padding: 25px; box-shadow: 0 8px 15px rgba(0,0,0,0.1); }
    </style>
</head>
<body class="bg-light">
    <nav class="navbar navbar-light bg-white shadow-sm px-4">
        <a class="navbar-brand text-primary font-weight-bold" href="{{ path('app_front_home') }}">
            <i class="fas fa-heart"></i> EmoNado
        </a>
        <a href="{{ path('app_rendez_vous_index') }}" class="btn btn-outline-primary btn-sm">
            <i class="fas fa-user-shield"></i> Accès Admin
        </a>
    </nav>

    <div class="hero-blue text-center">
        <h1 class="display-4 font-weight-bold">Espace Santé Patient</h1>
        <p class="lead">Consultez vos rendez-vous de manière sécurisée</p>
    </div>

    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-6 search-card text-center">
                <form action="{{ path('app_front_home') }}" method="get">
                    <div class="input-group">
                        <input type="text" name="cin" class="form-control" 
                               placeholder="Entrez votre numéro de CIN..." value="{{ cinSaisi }}">
                        <div class="input-group-append">
                            <button class="btn btn-primary px-4" type="submit">Valider</button>
                        </div>
                    </div>
                    <small class="text-muted mt-2 d-block">Identifiant unique requis pour protéger vos données.</small>
                </form>
            </div>
        </div>

        <div class="row mt-5">
            {% if rendez_vous is not empty %}
                {% for r in rendez_vous %}
                    <div class="col-md-4 mb-4">
                        <div class="card border-0 shadow-sm h-100" style="border-top: 4px solid #007bff;">
                            <div class="card-body">
                                <h5 class="text-primary text-uppercase font-weight-bold">{{ r.nomPatient }}</h5>
                                <p class="text-muted mb-2"><i class="fas fa-clock mr-2"></i>{{ r.date|date('d/m/Y H:i') }}</p>
                                <span class="badge badge-info">{{ r.type ? r.type.description : 'Consultation' }}</span>
                                <div class="mt-2 small text-success font-weight-bold">Status: {{ r.status }}</div>
                            </div>
                        </div>
                    </div>
                {% endfor %}
            {% elseif cinSaisi is not empty %}
                <div class="col-12 text-center py-4">
                    <div class="alert alert-warning">Aucun rendez-vous trouvé pour le CIN : {{ cinSaisi }}</div>
                </div>
            {% else %}
                <div class="col-12 text-center py-5">
                    <i class="fas fa-user-lock fa-3x text-light mb-3"></i>
                    <p class="text-muted">Veuillez saisir votre numéro CIN pour accéder à vos informations.</p>
                </div>
            {% endif %}
        </div>
    </div>
</body>
</html>