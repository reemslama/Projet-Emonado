<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Emonado Sante{% endblock %}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://kit.fontawesome.com/a2e1f6b6da.js" crossorigin="anonymous"></script>

    <style>
        body { 
            padding-top: 70px; 
            /* Arrière-plan personnalisé */
            background-image: url('{{ asset('images/background.png') }}');
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
            background-attachment: fixed;
            min-height: 100vh;
        }
        /* Style pour rendre le contenu lisible sur l'image */
        .main-container {
            background-color: rgba(255, 255, 255, 0.9); /* Fond blanc semi-transparent */
            border-radius: 15px;
            padding: 30px;
            box-shadow: 0px 4px 15px rgba(0,0,0,0.2);
        }
        .navbar-brand { font-weight: bold; }
        .btn-custom { border-radius: 50px; padding: 10px 30px; }
    </style>
</head>
<body>
<nav class="navbar navbar-expand-lg navbar-dark bg-success fixed-top">
    <div class="container">
        <a class="navbar-brand" href="#">Emonado</a>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto">
                <li class="nav-item"><a class="nav-link" href="#">Accueil</a></li>
                {# Garde ton bouton vers l'Admin ici #}
                <li class="nav-item">
                    <a class="btn btn-outline-light ms-lg-3" href="{{ path('app_rendez_vous_index') }}">Accès Admin</a>
                </li>
            </ul>
        </div>
    </div>
</nav>

<div class="container mt-5">
    <div class="main-container">
        {% block body %}
            {# Ton formulaire de recherche CIN actuel s'affichera ici #}
        {% endblock %}
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>